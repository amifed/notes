#include <iostream>
using namespace std;
const int N = 17;

int n, p[N];
int tk[N];

int main() {
    cin >> n;
    for (int i = 0; i < n; ++i) p[i] = i + 1;
    for (int i = 0; i < 1 << n; ++i) {
        int x = i;
        for (int j = 0; j < n; ++i) {
            
        }
    }
    return 0;
}